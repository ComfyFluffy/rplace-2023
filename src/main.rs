// use dotenvy::dotenv;

fn main() {
    env_logger::init();
    // dotenv().ok();
    // let instant = std::time::Instant::now();
    // get_max_min_coord();
    // println!("Time: {:?}", instant.elapsed());
    rplace_2023::run();
    // rplace_2023::find_never_updated_pixels();
    // rplace_2023::print_quad_circle();
}
